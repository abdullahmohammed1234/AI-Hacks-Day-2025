<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dashboard - Track2Give</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <%- include('partials/header') %>

    <main class="container dashboard-container">
      <!-- Welcome Section -->
      <section class="dashboard-welcome">
        <h1>Welcome back, <%= user.username %>! 👋</h1>
        <p class="dashboard-subtitle">
          Here's your food waste tracking dashboard
        </p>
      </section>

      <!-- Quick Stats -->
      <section class="stats-grid">
        <div class="stat-card">
          <div class="stat-icon stat-icon-primary">🍎</div>
          <div class="stat-content">
            <h3 class="stat-value"><%= foodItems.length %></h3>
            <p class="stat-label">Total Items</p>
          </div>
        </div>

        <div class="stat-card">
          <div
            class="stat-icon"
            style="
              background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
              width: 50px;
              height: 50px;
              border-radius: 12px;
              display: flex;
              align-items: center;
              justify-content: center;
              color: white;
              font-size: 1.5rem;
              margin-bottom: 1rem;
            "
          >
            ⚠️
          </div>
          <h3 style="margin: 0; color: #1f2937"><%= expiringItems.length %></h3>
          <p style="color: #6b7280; margin: 0.25rem 0 0 0">Expiring Soon</p>
        </div>

        <div class="stat-card">
          <div
            class="stat-icon"
            style="
              background: linear-gradient(135deg, #10b981 0%, #059669 100%);
              width: 50px;
              height: 50px;
              border-radius: 12px;
              display: flex;
              align-items: center;
              justify-content: center;
              color: white;
              font-size: 1.5rem;
              margin-bottom: 1rem;
            "
          >
            ♻️
          </div>
          <h3 style="margin: 0; color: #1f2937">
            <%= userImpact.itemsSaved %>
          </h3>
          <p style="color: #6b7280; margin: 0.25rem 0 0 0">Items Saved</p>
        </div>

        <div class="stat-card">
          <div
            class="stat-icon"
            style="
              background: linear-gradient(135deg, #06b6d4 0%, #0891b2 100%);
              width: 50px;
              height: 50px;
              border-radius: 12px;
              display: flex;
              align-items: center;
              justify-content: center;
              color: white;
              font-size: 1.5rem;
              margin-bottom: 1rem;
            "
          >
            🌱
          </div>
          <h3 style="margin: 0; color: #1f2937">
            <%= Math.round(userImpact.co2SavedKg * 10) / 10 %> kg
          </h3>
          <p style="color: #6b7280; margin: 0.25rem 0 0 0">CO2 Saved</p>
        </div>
      </section>

      <!-- Expiring Items Alert -->
      <% if (expiringItems.length > 0) { %>
      <section
        class="expiring-alert"
        style="
          background: #fef3c7;
          border-left: 4px solid #f59e0b;
          padding: 1.5rem;
          border-radius: 8px;
          margin: 2rem 0;
        "
      >
        <h3 style="color: #92400e; margin: 0 0 1rem 0">
          ⚠️ <%= expiringItems.length %> Item<%= expiringItems.length > 1 ? 's'
          : '' %> Expiring Within 3 Days
        </h3>
        <div class="expiring-items-list" style="display: grid; gap: 0.75rem">
          <% expiringItems.forEach(item => { const daysUntil = Math.ceil((new
          Date(item.expiryDate) - new Date()) / (1000 * 60 * 60 * 24)); %>
          <div
            style="
              background: white;
              padding: 1rem;
              border-radius: 6px;
              display: flex;
              justify-content: space-between;
              align-items: center;
            "
          >
            <div>
              <strong><%= item.name %></strong>
              <span style="color: #666; margin-left: 0.5rem"
                >• <%= item.quantity %> <%= item.unit %></span
              >
            </div>
            <span class="badge badge-warning">
              <%= daysUntil <= 0 ? 'Expired' : daysUntil === 1 ? 'Tomorrow' :
              daysUntil + ' days' %>
            </span>
          </div>
          <% }); %>
        </div>
        <div style="margin-top: 1rem">
          <a href="/donate" class="btn btn-primary">Donate These Items</a>
        </div>
      </section>
      <% } %>

      <!-- Main Content Grid -->
      <div class="dashboard-grid">
        <!-- My Food Items -->
        <section class="card dashboard-card">
          <div
            style="
              display: flex;
              justify-content: space-between;
              align-items: center;
              margin-bottom: 1.5rem;
            "
          >
            <h2 style="margin: 0">My Food Items</h2>
            <div class="dashboard-actions">
              <a href="/add-food-item" class="btn btn-secondary"
                >Add Manually</a
              >
              <a
                href="/scan"
                class="btn btn-primary"
              >
                📷 Scan Receipt
              </a>
            </div>
          </div>

          <% if (foodItems.length === 0) { %>
          <div style="text-align: center; padding: 3rem; color: #9ca3af">
            <p style="font-size: 3rem; margin: 0">📦</p>
            <p style="margin: 1rem 0 0 0">
              No food items yet. Add your first item or scan a receipt!
            </p>
          </div>
          <% } else { %>
          <div class="food-items-grid" style="display: grid; gap: 1rem">
            <% foodItems.forEach(item => { const daysUntil = Math.ceil((new
            Date(item.expiryDate) - new Date()) / (1000 * 60 * 60 * 24)); const
            urgencyClass = daysUntil <= 1 ? 'critical' : daysUntil <= 3 ?
            'warning' : 'success'; %>
            <div
              class="food-item-card"
              style="
                background: white;
                border: 1px solid #e5e7eb;
                border-radius: 8px;
                padding: 1rem;
                display: flex;
                justify-content: space-between;
                align-items: center;
              "
            >
              <div style="flex: 1">
                <div style="display: flex; align-items: center; gap: 0.75rem">
                  <span style="font-size: 1.5rem"
                    ><%= item.categoryIcon %></span
                  >
                  <div>
                    <h4 style="margin: 0"><%= item.name %></h4>
                    <p
                      style="
                        margin: 0.25rem 0 0 0;
                        color: #6b7280;
                        font-size: 0.875rem;
                      "
                    >
                      <%= item.quantity %> <%= item.unit %> • <%=
                      item.storageLocation %>
                    </p>
                  </div>
                </div>
              </div>
              <div style="text-align: right">
                <span class="badge badge-<%= urgencyClass %>">
                  <%= daysUntil <= 0 ? 'Expired' : daysUntil === 1 ? 'Tomorrow'
                  : daysUntil + ' days' %>
                </span>
                <p
                  style="
                    margin: 0.5rem 0 0 0;
                    color: #9ca3af;
                    font-size: 0.75rem;
                  "
                >
                  <%= new Date(item.expiryDate).toLocaleDateString() %>
                </p>
              </div>
            </div>
            <% }); %>
          </div>
          <% } %>

        </section>

        <!-- Nearby Shared Items -->
        <section class="card">
          <h2 style="margin: 0 0 1.5rem 0">🌍 Nearby Shared Items</h2>

          <% if (nearbySharedItems.length === 0) { %>
          <div style="text-align: center; padding: 2rem; color: #9ca3af">
            <p style="font-size: 2rem; margin: 0">🔍</p>
            <p style="margin: 1rem 0 0 0; font-size: 0.875rem">
              No shared items nearby
            </p>
          </div>
          <% } else { %>
          <div style="display: grid; gap: 1rem">
            <% nearbySharedItems.slice(0, 5).forEach(item => { %>
            <div
              style="
                background: #f9fafb;
                padding: 1rem;
                border-radius: 6px;
                border-left: 4px solid var(--primary-green);
              "
            >
              <div
                style="
                  display: flex;
                  justify-content: space-between;
                  align-items: start;
                "
              >
                <div>
                  <h4 style="margin: 0"><%= item.name %></h4>
                  <p
                    style="
                      margin: 0.25rem 0;
                      color: #6b7280;
                      font-size: 0.875rem;
                    "
                  >
                    <%= item.quantity %> <%= item.unit %>
                  </p>
                  <p
                    style="
                      margin: 0.5rem 0 0 0;
                      color: #9ca3af;
                      font-size: 0.75rem;
                    "
                  >
                    📍 <%= item.pickupLocation || 'Location not specified' %>
                  </p>
                </div>
                <a href="/community" class="btn btn-sm btn-primary">View</a>
              </div>
            </div>
            <% }); %>
          </div>
          <a
            href="/community"
            class="btn btn-secondary"
            style="width: 100%; margin-top: 1rem"
            >View All Shared Items</a
          >
          <% } %>
        </section>

        <!-- Environmental Impact -->
        <section class="card">
          <h2 style="margin: 0 0 1.5rem 0">🌱 Your Environmental Impact</h2>

          <div style="display: grid; gap: 1.5rem">
            <div class="impact-stat">
              <div
                style="
                  display: flex;
                  align-items: center;
                  gap: 0.75rem;
                  margin-bottom: 0.5rem;
                "
              >
                <span style="font-size: 1.5rem">💨</span>
                <h4 style="margin: 0; color: #1f2937">CO2 Saved</h4>
              </div>
              <p
                style="
                  margin: 0;
                  font-size: 1.5rem;
                  font-weight: 600;
                  color: var(--primary-green);
                "
              >
                <%= Math.round(userImpact.co2SavedKg * 10) / 10 %> kg
              </p>
              <p
                style="
                  margin: 0.25rem 0 0 0;
                  color: #6b7280;
                  font-size: 0.75rem;
                "
              >
                ≈ <%= Math.round(userImpact.co2SavedKg * 4.63) %> miles not
                driven
              </p>
            </div>

            <div class="impact-stat">
              <div
                style="
                  display: flex;
                  align-items: center;
                  gap: 0.75rem;
                  margin-bottom: 0.5rem;
                "
              >
                <span style="font-size: 1.5rem">💧</span>
                <h4 style="margin: 0; color: #1f2937">Water Saved</h4>
              </div>
              <p
                style="
                  margin: 0;
                  font-size: 1.5rem;
                  font-weight: 600;
                  color: #06b6d4;
                "
              >
                <%= Math.round(userImpact.waterSavedLiters).toLocaleString() %>
                L
              </p>
              <p
                style="
                  margin: 0.25rem 0 0 0;
                  color: #6b7280;
                  font-size: 0.75rem;
                "
              >
                ≈ <%= Math.round(userImpact.waterSavedLiters / 8) %> days of
                drinking water
              </p>
            </div>

            <div class="impact-stat">
              <div
                style="
                  display: flex;
                  align-items: center;
                  gap: 0.75rem;
                  margin-bottom: 0.5rem;
                "
              >
                <span style="font-size: 1.5rem">💰</span>
                <h4 style="margin: 0; color: #1f2937">Money Saved</h4>
              </div>
              <p
                style="
                  margin: 0;
                  font-size: 1.5rem;
                  font-weight: 600;
                  color: #f59e0b;
                "
              >
                $<%= Math.round(userImpact.moneySavedDollars * 100) / 100 %>
              </p>
              <p
                style="
                  margin: 0.25rem 0 0 0;
                  color: #6b7280;
                  font-size: 0.75rem;
                "
              >
                From <%= userImpact.itemsSaved %> items saved
              </p>
            </div>
          </div>
        </section>
      </div>

      <!-- Quick Actions -->
      <section
        style="
          background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
          color: white;
          padding: 2rem;
          border-radius: 12px;
          margin: 2rem 0;
          text-align: center;
        "
      >
        <h2 style="margin: 0 0 1rem 0; color: white">Quick Actions</h2>
        <div
          style="
            display: flex;
            gap: 1rem;
            justify-content: center;
            flex-wrap: wrap;
          "
        >
          <a
            href="/recipes"
            class="btn"
            style="background: white; color: #667eea; font-weight: 600;"
            >🍳 Find Recipes</a
          >
          <a
            href="/donate"
            class="btn"
            style="background: white; color: #667eea; font-weight: 600;"
            >💚 Donate Food</a
          >
          <a
            href="/community"
            class="btn"
            style="background: white; color: #667eea; font-weight: 600;"
            >🌍 Community</a
          >
          <a
            href="/settings"
            class="btn"
            style="background: white; color: #667eea; font-weight: 600;"
            >⚙️ Settings</a
          >
        </div>
      </section>
    </main>

    <%- include('partials/footer') %>
  </body>
</html>
