<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Track2Give - Stop Food Waste, Save Money, Save the Planet</title>
    <link rel="stylesheet" href="/style.css" />

    <style>
      /* Page-specific styles for landing page */

      .hero {
        min-height: 100vh;
        background: linear-gradient(135deg, #10b981 0%, #f59e0b 100%);
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
        padding: 8rem 2rem 4rem;
        position: relative;
        overflow: hidden;
      }

      .hero::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: url('data:image/svg+xml,<svg width="100" height="100" xmlns="http://www.w3.org/2000/svg"><circle cx="50" cy="50" r="2" fill="white" opacity="0.1"/></svg>');
        animation: float 20s linear infinite;
      }

      @keyframes float {
        from {
          transform: translateY(0);
        }
        to {
          transform: translateY(-100px);
        }
      }

      .hero-content {
        max-width: 800px;
        position: relative;
        z-index: 2;
      }

      /* Shared container to align sections consistently */
      .section-inner {
        max-width: 1100px;
        margin: 0 auto;
        padding: 0 2rem;
        box-sizing: border-box;
      }

      .hero h1 {
        font-size: 3.5rem;
        color: white;
        margin-bottom: 1.5rem;
        animation: fadeInUp 0.8s ease;
      }

      .hero p {
        font-size: 1.3rem;
        color: rgba(255, 255, 255, 0.9);
        margin-bottom: 2rem;
        animation: fadeInUp 0.8s ease 0.2s backwards;
      }

      @keyframes fadeInUp {
        from {
          opacity: 0;
          transform: translateY(30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .cta-button {
        display: inline-block;
        padding: 1rem 3rem;
        background: white;
        color: #10b981;
        text-decoration: none;
        border-radius: 50px;
        font-weight: bold;
        font-size: 1.1rem;
        transition: all 0.3s;
        animation: fadeInUp 0.8s ease 0.4s backwards;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        border: 2px solid white;
        box-sizing: border-box;
      }

      .cta-button:hover,
      .cta-button:focus {
        transform: translateY(-3px);
        box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
        outline: none;
        background: #f0fdf4;
      }

      .cta-button:active {
        transform: translateY(-1px);
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        background: #dcfce7;
      }

      .cta-button-secondary {
        display: inline-block;
        padding: 1rem 3rem;
        background: rgba(255, 255, 255, 0.2);
        color: white;
        text-decoration: none;
        border-radius: 50px;
        font-weight: bold;
        font-size: 1.1rem;
        transition: all 0.3s;
        animation: fadeInUp 0.8s ease 0.4s backwards;
        border: 2px solid white;
        margin-left: 1rem;
        box-sizing: border-box;
      }

      .cta-button-secondary:hover,
      .cta-button-secondary:focus {
        background: rgba(255, 255, 255, 0.3);
        transform: translateY(-3px);
        outline: none;
        box-shadow: 0 5px 20px rgba(255, 255, 255, 0.3);
      }

      .cta-button-secondary:active {
        transform: translateY(-1px);
        background: rgba(255, 255, 255, 0.4);
        box-shadow: 0 2px 10px rgba(255, 255, 255, 0.2);
      }

      .cta-buttons {
        display: flex;
        gap: 1rem;
        justify-content: center;
        flex-wrap: wrap;
      }

      .stats {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 2rem;
        margin-top: 2rem;
      }

      .stat-card {
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        padding: 1rem;
        border-radius: 20px;
        border: 1px solid rgba(255, 255, 255, 0.2);
      }

      .stat-number {
        font-size: 2rem;
        font-weight: bold;
        color: white;
      }

      .stat-label {
        color: rgba(255, 255, 255, 0.9);
        margin-top: 0.5rem;
      }

      .features {
        padding: 6rem 5%;
        background: #f9fafb;
      }

      .section-title {
        text-align: center;
        font-size: 2.5rem;
        margin-bottom: 3rem;
        color: #1f2937;
      }

      .feature-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 3rem;
        margin: 0 auto;
      }

      .feature-card {
        background: white;
        padding: 2.5rem;
        border-radius: 20px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        transition: transform 0.3s, box-shadow 0.3s;
      }

      .feature-card:hover {
        transform: translateY(-10px);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
      }

      .feature-icon {
        font-size: 3rem;
        margin-bottom: 1rem;
      }

      .feature-card h3 {
        font-size: 1.5rem;
        margin-bottom: 1rem;
        color: #1f2937;
      }

      .feature-card p {
        color: #6b7280;
        line-height: 1.8;
      }

      .impact {
        padding: 6rem 5%;
        /* solid green->amber gradient background */
        background: linear-gradient(135deg, #10b981 0%, #f59e0b 100%);
        color: white;
        text-align: center;
        position: relative;
      }

      .impact-content {
        max-width: 800px;
        margin: 0 auto;
        position: relative;
        z-index: 2;
      }

      .impact h2 {
        font-size: 2.5rem;
        margin-bottom: 2rem;
      }

      /* Impact heading should be plain white to contrast the gradient background */
      .impact h2 {
        color: white;
      }

      .impact p {
        font-size: 1.2rem;
        margin-bottom: 3rem;
        opacity: 0.95;
      }

      .cta-secondary {
        display: inline-block;
        padding: 1rem 3rem;
        background: white;
        color: #059669;
        text-decoration: none;
        border-radius: 50px;
        font-weight: bold;
        font-size: 1.1rem;
        transition: all 0.3s;
      }

      .cta-secondary:hover {
        transform: scale(1.05);
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
      }

      footer {
        background: #1f2937;
        color: white;
        padding: 3rem 5%;
        text-align: center;
      }

      footer p {
        opacity: 0.8;
      }

      @media (max-width: 768px) {
        .hero h1 {
          font-size: 2.5rem;
        }

        .cta-buttons {
          flex-direction: column;
          align-items: center;
          gap: 0.75rem;
        }

        .cta-button,
        .cta-button-secondary {
          margin: 0 !important;
          width: 100%;
          max-width: 300px;
          text-align: center;
          display: block;
          padding: 1rem 2rem;
        }

        .nav-links {
          display: none;
        }

        .stats {
          grid-template-columns: 1fr;
        }
      }

      @media (max-width: 480px) {
        .cta-button,
        .cta-button-secondary {
          max-width: 100%;
          font-size: 1rem;
          padding: 0.875rem 1.5rem;
        }

        .hero {
          padding: 6rem 1.5rem 3rem;
        }
      }

      /* Add after your existing styles but before the @media query */
      .section-divider {
        height: 400px;
        width: 100%;
        background-size: cover;
        background-position: center;
        background-attachment: fixed;
        position: relative;
      }

      .section-divider::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: rgba(0, 0, 0, 0.3);
      }

      .divider-1 {
        background-image: url("https://images.unsplash.com/photo-1490818387583-1baba5e638af?auto=format&fit=crop&q=80");
      }

      .divider-2 {
        background-image: url("https://images.unsplash.com/photo-1498837167922-ddd27525d352?auto=format&fit=crop&q=80");
      }
    </style>
  </head>
  <body>
    <%- include('partials/header', { user: user || null, currentPage: 'home' })
    %>

    <section class="hero">
      <div class="section-inner">
        <div class="hero-content">
          <h1>Stop Wasting Food.<br />Start Saving Money.</h1>
          <p>
            Track your groceries, get expiry alerts, and discover recipes for
            food about to go bad. Join the movement to reduce food waste and
            fight climate change.
          </p>
          <div class="cta-buttons">
            <a href="/register" class="cta-button">Get Started Free</a>
            <a href="/login" class="cta-button-secondary">Login</a>
          </div>

          <div class="stats">
            <div class="stat-card">
              <div class="stat-number">1/3</div>
              <div class="stat-label">of food produced is wasted globally</div>
            </div>
            <div class="stat-card">
              <div class="stat-number">$1,500</div>
              <div class="stat-label">average family loses per year</div>
            </div>
            <div class="stat-card">
              <div class="stat-number">8-10%</div>
              <div class="stat-label">
                of global greenhouse gases from food waste
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
    <div class="section-divider divider-1"></div>

    <section id="features" class="features">
      <div class="section-inner">
        <h2 class="section-title">Smart Features, Real Impact</h2>
        <div class="feature-grid">
          <div class="feature-card">
            <div class="feature-icon">📱</div>
            <h3>Smart Tracking</h3>
            <p>
              Scan receipts or manually add items. We'll track expiry dates and
              notify you before food goes bad.
            </p>
          </div>
          <div class="feature-card">
            <div class="feature-icon">🍳</div>
            <h3>Recipe Suggestions</h3>
            <p>
              Get personalized recipes based on ingredients expiring soon. Turn
              waste into delicious meals.
            </p>
          </div>
          <div class="feature-card">
            <div class="feature-icon">📊</div>
            <h3>Impact Dashboard</h3>
            <p>
              See your environmental impact: CO₂ saved, water conserved, and
              money not wasted.
            </p>
          </div>
          <div class="feature-card">
            <div class="feature-icon">🤝</div>
            <h3>Community Sharing</h3>
            <p>
              Connect with neighbors to share or donate excess food before it
              expires.
            </p>
          </div>
          <div class="feature-card">
            <div class="feature-icon">🔔</div>
            <h3>Smart Alerts</h3>
            <p>
              Timely notifications so nothing gets forgotten in the back of your
              fridge.
            </p>
          </div>
          <div class="feature-card">
            <div class="feature-icon">🎯</div>
            <h3>Shopping Lists</h3>
            <p>
              Plan better with AI-powered shopping lists that prevent
              overbuying.
            </p>
          </div>
        </div>
      </div>
    </section>

    <div class="section-divider divider-2"></div>

    <section id="impact" class="impact">
      <div class="section-inner">
        <div class="impact-content">
          <h2>Your Actions, Real Change</h2>
          <p>
            Every meal saved is a step toward a sustainable future. Track2Give
            helps you contribute to UN Sustainable Development Goal 12:
            Responsible Consumption and Production.
          </p>
          <p>
            Together, we can reduce the 1.3 billion tons of food wasted annually
            while saving money and protecting our planet.
          </p>
        </div>
      </div>
    </section>

    <%- include('partials/footer') %>
  </body>
</html>
